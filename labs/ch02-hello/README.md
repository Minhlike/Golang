# Lab chương 2 - hello

Từ thư mục này, chạy `go run .`. Đổi giá trị `name`, chạy lại, rồi cố ý đổi
`fmt` thành tên package không tồn tại. Hãy đọc toàn bộ lỗi compiler trước khi
sửa. Khi đã hiểu, chạy `go fmt .` và `go vet .`.

