# Lab: đọc một tệp Go

Chạy lần lượt `go run .`, `go test ./...` và `go vet ./...`. Sau đó dự đoán lỗi
trước khi tự thử: đổi `status := 503` thành `status = 503`, rồi thay kiểu tham số
của `severity` thành `string` nhưng giữ nguyên lời gọi.
