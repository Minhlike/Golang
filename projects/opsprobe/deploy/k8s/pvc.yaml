apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opsprobe-data-pvc
  namespace: default
  labels:
    app.kubernetes.io/name: opsprobe
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
